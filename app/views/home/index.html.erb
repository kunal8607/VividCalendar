
<div class="jumbotron">
	<h1>Vivid Calendar</h1>
  <% if user_signed_in? %>
    <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-danger rounded-0" %>
  <% else %>
      <%= link_to "Sign in with Google to see the calandar", user_google_oauth2_omniauth_authorize_path, class: "btn btn-danger" %>
    <% end %>
</div>
